{
    "builds": [
      { "src": "main.py", "use": "@vercel/python"}
     
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/main.py" }
    ],
    "build": {
    "commands": [
      "./build.sh"
    ]
  },
  "functions": {
    "api/*.py": {
      "runtime": "python3.10"
    }
  }
  }